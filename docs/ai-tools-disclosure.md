# AI tools disclosure

> **Owner: please read and edit before submitting.** The factual sections below describe what the
> tooling produced and are accurate as written. The section *"What I decided and reviewed"* makes
> claims about your own involvement — check every line against what you actually did, and cut
> anything you would not be comfortable defending in the room. An overstated disclosure is worse
> than a modest one.

## Tools used

| Tool | Model | What it was used for |
|---|---|---|
| **Claude Code** (Anthropic) | Claude Opus 4.5 | Writing the implementation: Java source, SQL migrations, React/TypeScript components, tests, configuration and this documentation |
| **Google Gemini** | `gemini-2.5-flash` | Not a build tool — it is the AI capability *inside* the shipped product, called at runtime by the analysis pipeline |

No other code-generation tool was used. No code was copied from an existing project of mine or from
a public repository; the application was built from scratch during this effort, on top of the
open-source frameworks listed in [`LIBRARIES.md`](../LIBRARIES.md).

## What was generated by Claude Code

Effectively all of the source in this repository was written by Claude Code, working from the build
plan in `AI_FUTURE_PROCESS_DESIGNER_BUILD_PLAN.md`:

- The Flyway migrations, the JPA entity model and the repository layer.
- The analysis pipeline: knowledge retrieval, the prompt template renderer, the Gemini provider,
  the response parser, the payload validator, the persistence stage and the run recorder.
- The REST controllers, DTOs, the RFC 7807 error handling and the configuration properties.
- The prompt templates in `backend/src/main/resources/prompts/`.
- The Next.js frontend in its entirety.
- The 84 tests, including the integration suite that runs against a real PostgreSQL.
- The Dockerfile, Render blueprint, CI workflow, and the documents in `docs/`.

The AssessWise sample data — six processes, their activities, roles, systems and recorded pain
points — was also generated, informed by how online-assessment operations typically work. The
figures in it (review turnaround times, proctor-to-candidate ratios, ticket volumes) are illustrative
of a mid-size assessment operation and are **not** measurements of any real organisation.

## What was not simply accepted

Some things the tooling got wrong or left in a weaker state, and were corrected during the build.
These are recorded because they are the honest texture of building this way:

- **A detached-entity bug in the run recorder.** The first version passed a `KnowledgeSnippet`
  loaded in an earlier transaction into an entity whose primary key is derived from it via
  `@MapsId`. Every analysis failed with a 500. Caught by the integration tests, fixed by resolving a
  managed reference inside the writing transaction.
- **A `@Transactional` method invoked from within the same bean.** Spring applies transactions
  through a proxy, so the annotation silently did nothing. The loader was extracted into its own
  bean, `AnalysisInputLoader`.
- **A stemmer that didn't collide the forms it needed to.** "grading" and "grade" produced different
  tokens, so keyword retrieval missed obvious matches. Found by a unit test written to assert the
  property rather than the implementation.
- **The initial model choice.** The build plan specified `gemini-1.5-flash`; that is no longer the
  current free-tier default, so the default became `gemini-2.5-flash` — and the model id became
  configuration rather than a constant.
- **Source URLs were verified, not assumed.** Every one of the 16 research URLs was fetched and
  checked to return HTTP 200 before being written into the seed data. Several plausible-looking
  candidates were discarded because they 404'd or blocked access.

## What I decided and reviewed

*Owner: rewrite this section in your own words. The points below are the decisions that were made
during the build and that you will be asked to defend — keep the ones you genuinely own, and be
straightforward about the rest.*

- **The domain and scope.** Online education and digital assessment, six seed processes, one
  pipeline. Chosen so that I can tell whether the generated output is actually sensible for the
  domain, rather than merely plausible-sounding.
- **Curated research instead of live web search.** A deliberate trade-off: reproducible and
  auditable, at the cost of a small static corpus. The reasoning and the limitations are written
  down in [`sources.md`](sources.md) rather than glossed over.
- **One live AI provider behind an interface, and no runtime failover.** The brief asks for the
  "what if it stops being free" risk to be *explained*. Building a second provider would have looked
  impressive and consumed time that the pipeline needed more.
- **The traceability tables.** `analysis_run` storing the exact prompt and raw response is not in
  the original plan. It was added because "outputs are traceable" and "nothing is hard-coded" are
  claims that should be checkable with a SQL query, not taken on trust.
- **Structured output plus local validation, not either alone.** The Gemini response schema reduces
  how often the response is malformed; the validator and repair retry still run every time, because
  a provider that silently ignored the schema must not be able to corrupt the database.
- **Failure behaviour as a product feature.** Missing API key, exhausted quota, unusable model
  output and a concurrent analysis each produce a distinct status code and an actionable message.
  This was specified up front, not retrofitted.

## How to verify any of this

The repository is the evidence. Every claim above is checkable:

```bash
cd backend && ./mvnw verify     # 84 tests, real PostgreSQL, real migrations
git log --oneline               # the build history
```

And at runtime, the prompt and raw model response for any analysis are one click away in the UI, or
one query away in the database:

```sql
SELECT model, repair_attempted, prompt_tokens, output_tokens, duration_ms, prompt_text, raw_response
FROM analysis_run ORDER BY started_at DESC LIMIT 1;
```
