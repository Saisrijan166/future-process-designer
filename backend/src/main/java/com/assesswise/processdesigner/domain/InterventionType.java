package com.assesswise.processdesigner.domain;

/** The kind of change an AI intervention makes to the current process. */
public enum InterventionType {
    AUTOMATE,
    AUGMENT,
    ELIMINATE,
    NEW
}
